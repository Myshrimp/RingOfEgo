using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName ="scriptable",fileName ="pars_scr")]
public class scriptable_Pars : ScriptableObject
{
    public int id;
    public string name_;
    public int level;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
